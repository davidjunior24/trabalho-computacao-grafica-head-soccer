#ifndef UPDATE_H
#define UPDATE_H

//=====================================
// UPDATE -- loop principal do jogo (fisica, entrada, IA, animacoes)
//=====================================

// Callback de timer do GLUT, chamado a cada 16ms (~60fps).
void update(int valor);

#endif // UPDATE_H
